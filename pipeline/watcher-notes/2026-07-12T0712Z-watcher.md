# Watcher note — 2026-07-12T07:12Z (results-watcher, unattended, Fable 5)

**Why this is a separate file and not a WAR_LOG append:** a second session was
writing the same deliverables *during this run* (see "Concurrency" below) and holds
`.git/index.lock` (owned by another uid — `rm` returns "Operation not permitted",
so the task's rm-lock path does not apply). Appending to the append-at-top WAR_LOG
while another writer rewrites it risks a clobber, and I cannot commit anyway. So I
stood down from editing shared files and recorded findings here. Fold into WAR_LOG
when the repo is quiet.

## Concurrency detected (decision: do not interfere)
Between ~07:04–07:10Z the following were written by an external (Mac-host) session:
`summary.md` (07:04), `README.md`, `index.html`, `figure_significance.py`,
`figures/significance_wall.{png,svg}`, `_remotion/out/video.mp4` (07:06),
`deliverables/brakepoint_video.mp4` (07:07, 16,025,681 B — copied from the render), and a
0-byte `.git/index.lock` (07:10). `summary.md` changed between my own read and
write. Conclusion: an active foreground session is polishing + rendering. I did
**not** clobber it, fight the lock, or commit. All work below is read-only
verification + this note.

## Verified healthy (read-only)
- **Smokes green:** `python3 pipeline/edistance_core.py` → PASSED (unbiased E-dist +
  permutation E-test). `python3 pipeline/direction.py` → PASSED (sign recovered;
  tiers + donor sign-agreement correct).
- **No residual overclaim:** `"drug-target quadrant"` appears only in historical
  WAR_LOG entries — **zero** occurrences in any live deliverable (`.py/.html/.svg/.tsx`).
  The pass-#7 hero-figure fix is confirmed applied.
- **IL2RB correctly demoted:** absent from README / summary / index.html /
  demo_storyboard (it is a non-shortlisted network note in the finding). Framing is
  the genome-scale signed causal map + 5-candidate brake shortlist (lead CBLB).
- **Dossiers:** IL2RB, VAV2, BLNK, CBLB all present and valid JSON (task item 4 —
  nothing to enrich; all four cached & recent).
- **New `significance_wall` figure renders honestly:** n=11,438 tested, 97.5% clear
  q<0.05, 9,802 at the permutation floor — the data-driven argument for effect-size
  ranking. Good addition; keep.

## HeyGen video poll (task item 1)
`get_project_status(60cf1164-…)` → `sessionStatus: completed`, BUT it is the **old
80.9s IL2RB-framed silent cut** with **21 lint errors** and **validate skipped**.
It predates the target-discovery pivot. **Recommendation: do NOT resurrect it** —
the primary video is the local Remotion `deliverables/brakepoint_video.mp4` (freshly re-rendered
this window, 16 MB). Refining 60cf1164 via `compose` would re-introduce the
abandoned IL2RB story and duplicate the better, controlled local cut.

## Flags for Sam / next quiet pass
1. **[HARD-GATE] `deliverables/summary.md` is 267 words** — over the 100–200
   submission cap. The current (good) 4-paragraph version needs a trim. A ready,
   honesty-preserving **179-word** block is provided below — paste this (or trim the
   live file to match) before submitting.
2. **`deliverables/VIDEO_URL.txt` is missing** — GOAL.md cites it as the video
   "source of truth." Recommend restoring it: primary = local `brakepoint_video.mp4` (Remotion,
   Brakepoint causal-map cut); alternates = HeyGen `29939a8d` (6-beat) and
   `60cf1164` (old IL2RB, weak/superseded). Presigned HeyGen URLs expire, so store
   projectIds + a "re-fetch via get_render_status" note rather than a stale URL.
3. **Video source is one iteration ahead of any committed mp4 only if not yet
   committed** — the concurrent session already rendered the new `brakepoint_video.mp4`; just
   ensure it gets committed (I could not, lock held).
4. **CAMERA/cameraPR robustness (pass-#7 item 2) still un-taken** — optional; leave
   the frozen p=0.70 Mann–Whitney as-is unless Sam wants the competitive-test
   upgrade folded in (Sam's call on touching the locked finding this close to
   deadline).

## Ready-to-paste submission summary (179 words, honesty-preserving)
> Using **Claude Science**, we screened for druggable **brakes** on human CD4⁺
> T-cell function — the target class behind checkpoint-blockade immunotherapy. From
> a genome-scale CRISPRi Perturb-seq screen (**2,638,736 cells, 12,449 knockdowns**;
> Gladstone/Marson, two of four donors), we build a **signed causal map** and read a
> target shortlist off it. Standard Perturb-seq ranks by significance — but at 2.6 M
> cells **97.5%** of knockdowns clear q<0.05, ranking almost nothing. We instead
> rank by **causal effect size** (power-equalized energy distance) and add the axis
> standard analysis skips: a per-cell **direction-of-effect** score (an 8-hour
> effector-minus-checkpoint transcriptional read-out, not a functional assay).
> Magnitude alone nominates the cell's own machinery; the signed axis reclassifies
> it — 14 of the 15 largest, donor-consistent effects are the TCR module, correctly
> negative — and surfaces candidate brakes. We prioritize **five candidates for
> validation** — CBLB, CD5, DGKA, SMAD3, UBASH3A — by convergent evidence. Lead
> **CBLB**: inhibitors in early-phase trials (NX-1607, HST-1011) and autoimmune
> genetics. Honestly: at two donors the quadrant shows **no significant
> brake-enrichment** (p=0.70) and three candidates are donor-split — a prioritized
> hypothesis for the full cohort, not a finished list. All reproducible code.

### NEEDS SAM
- Commit the in-flight work (I couldn't — `.git/index.lock` held by the active
  session). Verify the new `brakepoint_video.mp4` + `significance_wall` figure + `index.html`
  upgrade land in a commit.
- Trim `summary.md` to ≤200 words (block above) — hard submission gate.
- Restore `deliverables/VIDEO_URL.txt`.
- Hard walls untouched this run: no CV-platform submit, no push, no creds, no
  connector auth.
