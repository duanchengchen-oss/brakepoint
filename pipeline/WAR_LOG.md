# WAR LOG — Built with Claude: Life Sciences (research track, solo)
Append-only, newest first. Self-critique loop: every 8h. Results-watcher: every 6h.

## 2026-07-12T (Day 6) — Fable 5 (Claude Code on the Mac): REAL direction axis computed on the genome-scale data; hero reframed to the signed causal map; P0-B honesty correction
_Running in Claude Code on Sam's MacBook (NOT the DGX, contrary to the finish-prompt's assumption) — but the DGX (`spark-hk`, GB10) is SSH-reachable and holds the real data, so I finished the science the sandbox couldn't. User directive this session: quality bar is "impress the judges; ship nothing improvable."_

**Ground truth established (verify-don't-assume):**
- The genome-scale CD4 data lives on the DGX: `/home/sam/tcell_data/GWCD4i_Stim8hr_D1D2.built.h5ad` (2,638,736 cells × 4,816 HVGs, raw counts, Ensembl var, obs=`perturbation`+`donor`, control=`control`) + `.scvi.h5ad` (X_scVI). DGX env `~/miniforge3/envs/tcell` (scanpy 1.12; use `PYTHONNOUSERSITE=1` — a broken user-site pandas shadows it).
- **P0-B RESOLVED — there is NO Gladstone-provided PPI network and NO Decima/Performer regulatory model.** The authoritative `data_sharing_readme.md` documents the provided artifacts: Perturb-seq expression (cell + pseudobulk), a genome-wide **DESeq2 DE result** (`GWCD4i.DE_stats.h5ad`), and supplementary signature/validation tables (Th1/Th2 polarization, autoimmune-cluster enrichment, arrayed validation, aging). No interaction network, no regulatory model — those were never provided. → The long-standing caveat ("provided PPI/regulatory not used yet") is **misframed and must be corrected everywhere**: STRING v12 + Open Targets are legitimate *public* layers on top of the provided data, not substitutes for withheld provided data. (README line 38, index.html lines 142/161/214, hero_dossier §5, gladstone-datasets-integration.md all carry the false claim — fixing.)

**Did — P0-A, the carried #1 gap, now REAL on 2.64M cells:**
1. `direction.py` — added a **CD4-appropriate signed axis** (`CD4_EFFECTOR_GENES` 16: IFNG/IL2/TNF/CSF2/LTA/XCL1-2/CCL3-4/GZMB/TNFRSF9/CD69/MYC/IRF4/BATF/TBX21 vs `CD4_DYSFUNCTION_GENES` 13: PDCD1/CTLA4/LAG3/HAVCR2/TIGIT/BTLA/CD160/VSIR/ENTPD1/TOX/NR4A1-3). CD8 cyto/exh kept as default so `_smoke` is unchanged. **Vectorized `score_direction`** (bincount over integer group codes) — 12.4k perts × 2.64M cells now aggregates in ~1s (was ~30 min of object-array masking); numpy-only, identical smoke output.
2. `direction_genomescale.py` (NEW, runs on DGX) — streams the built h5ad in row-chunks (never materializes the ~5B-nnz matrix), scores `mean(log-norm effector) − mean(log-norm dysfunction)` per cell (background cancels), reuses `score_direction`. **Ran in 38.9s**, both modules 16/16 + 13/13 present. Output → `outputs_gladstone/direction_scores_raw.csv` + `direction_meta.json`.
3. `merge_direction.py` (NEW) — merges the signed score into `ranked_perturbations.csv` + assigns `direction_tier` via the unit-tested `assign_tier` (tau=0.05). Tier counts: brake 2308 / enhancer 2972 / required-machinery 2542 / neutral 4627.

**Result — the signed map validates itself and reframes the hero:**
- The 9 largest E-distance effects are ALL strongly **negative** direction (donor-agreed): ZAP70 −0.63, CD3E −0.66, CD3D −0.66, CD3G −0.70, PLCG1 −0.66, LAT −0.64, VAV1 −0.59, LCP2 −0.76, CD247 −0.49 → the TCR machinery is correctly flagged **required-machinery, not druggable** (magnitude alone would headline them).
- Known immune **brakes trend positive**: CBLB +0.14, CD5 +0.15, DGKA +0.08, UBASH3A +0.05, CDKN1B +0.06, TOX +0.18, CTLA4≈0. Highest-magnitude viable positive candidate with sensible biology = **SMAD3** (E-dist 25, TGF-β effector brake; donor sign-agreement is False — caveat).
- **IL2RB reframed:** KD is ~lethal (viability 0.13, direction −0.24) → required machinery for the pro-survival IL-2 signal. This is exactly why the therapeutic is an IL-2/CD122 **agonist** (aldesleukin, N-803/Anktiva), NOT an inhibitor — the direction data now *explains* IL2RB instead of resting on a STRING nomination.

**Decision (reframe):** headline pivots from "STRING→IL2RB network nomination" (the most attackable part) to **the genome-scale signed causal map** (novel, visual, self-validating). Present a brake shortlist (validated CBLB/CD5/DGKA/UBASH3A + higher-magnitude SMAD3/TGF-β and IL-2/IL-12 axis); keep IL2RB as an honestly-caveated convergence node (network + agonist-axis). Rebuild all deliverables (figures/landing/video/summary/README) on this, to a high visual bar.

### NEEDS SAM (unchanged hard walls)
- Record the narrated video VO + live Claude Science provenance capture (beats 2 & 5).
- Final submission click. (Repo push now unblocked — `gh` authed as duanchengchen-oss; will create/push after the rebuild lands.)

## 2026-07-11T14:05Z (Day 5, 22:05 HKT) — VIDEO REBUILT for quality (Sam live: "that video was horrible, very low quality") → hand-built 1080p cut + narration deck
_Sam returned mid-run and rejected the HeyGen auto-cut; asked for "1 2 3 together." A **parallel Cowork session** (13:35Z entry below) had meanwhile shipped `direction_axis.svg` + the T1D→autoimmune fix + verified the HeyGen renders — I built on that; **net-new here = a genuinely high-quality demo I control end-to-end + a narration deck**. Sandbox network is locked (npm/pip 403 → no pptxgenjs/Canva-native install), so I used **preinstalled matplotlib → ffmpeg**. No hard walls crossed (no submit/push/creds/connectors)._

**Did:**
1. **PRIMARY video — `deliverables/IL2RB_demo.mp4`** (1920×1080, **2:24**, 6 beats, gentle fades). Slides hand-designed in matplotlib on the **hero_convergence teal/navy brand** (no purple, no emoji); encoded per-clip + concat-copy (xfade too heavy for the 45s shell window). **Real numbers only** — validation chart pulls genome-scale `outputs_gladstone` E-distances (ZAP70 73.3 … CD3D 47.2); RASA2 flagged as the **CD8-arm** cross-check. Every slide visually QA'd (fixed chip/pillar overflow, hero-node caption, band collision) + frames QA'd from the encoded mp4.
2. **Narration deck — `deliverables/IL2RB_demo_deck.pptx`** (validates clean): same 6 slides full-bleed + **verbatim VO in the speaker notes** (TCR gene list matched to slide; IL2RB genetics = "MS + autoimmune"). Plus `IL2RB_demo_slides.pdf` + `video_slides/`.
3. **HeyGen (route 3 → b-roll):** my `compose` refined the original project (**60cf1164 → new render 3a8adaa1**, COMPLETED); the parallel session's full 6-beat cut (**29939a8d / 8e3a4436**) also COMPLETED. Both re-verified via get_render_status this run. `VIDEO_URL.txt` rewritten to lead with the local cut + no-re-render refresh instructions.
4. **Landing page:** embedded the mp4 in a new dark **#demo** section (poster = slide 1) + footer links (demo · deck · Canva infographic · repo); hero button → `#demo`.
5. **INFOGRAPHIC:** Canva `DAHPEnceXZE` exported to PNG (job success) but the **network lockout blocks pulling the binary into the repo** → landing page references the **Canva view link** instead. (Local embed needs Sam to drop the PNG in `deliverables/`.)
6. **Pipeline upkeep (subagent):** `edistance_core.py` + `direction.py` smokes **PASS**; direction.py verified complete; added `direction_figure_spec.md` + guarded `direction_figure.py`; dossiers IL2RB/VAV2/BLNK/CBLB re-validated.

**Decisions:** hand-built matplotlib/ffmpeg mp4 over a Canva-native export (Sam flagged quality + I control every pixel + network lockout blocked installs); kept it **silent/caption-driven at 2:24** since the narrated submission needs Sam's VO + live CS capture regardless; demoted HeyGen to b-roll rather than deleting (verified, free insurance).

### NEEDS SAM
- **[the scored artifact]** Record VO onto `IL2RB_demo_deck.pptx` (script in the notes) + screen-capture the **live Claude Science provenance** for beats 2 (reviewer) & 5 (hero) — the "show us how CS got you there" moment no agent can drive.
- **[hard walls]** `REPO_URL` + hosted `DEMO_URL`, create/push the public repo, final submit (local commits done).
- **[minor]** Drop the Canva infographic PNG into `deliverables/` if a local embed is wanted (agent is network-blocked from downloading it).
- **[carried]** Signed `direction_score` CS re-run to populate the real 2-axis figure (direction.py authored + unit-tested; no scanpy/GPU here).

## 2026-07-11T13:35Z (Day 5, 21:35 HKT) — build pass: REAL video kicked off (honest ID correction), 2-axis direction figure shipped, T1D→autoimmune accuracy fix
_Cowork main session, numpy-only sandbox. No hard walls crossed (no submit / push / creds / connectors). Smokes all green (edistance_core, direction, concordance)._

**Did:**
1. **3-min video — verified, plus a self-correction.** **Two REAL renders** now exist, both confirmed `renderStatus=COMPLETED` via `get_render_status` (presigned URLs returned by the tool verbatim → saved to `deliverables/VIDEO_URL.txt`): (a) **60cf1164 / 4327a9ea** — the earlier **autonomous watcher** cut (65.7s, 8 scenes); its previously-saved MP4 URL **matches the live tool output byte-for-byte**; (b) **29939a8d / 8e3a4436** — a fuller **6-beat** cut I composed this session. **Correcting my own earlier claims (both WRONG):** (i) I called 60cf1164 a "fabricated placeholder" — it is in fact a genuine watcher-made render (created 11:01Z, before this session); (ii) I then suspected its saved MP4 URL was fabricated — it verifies exactly. Net: the watcher did correct work; my only real error was misreading the transcript and composing a **redundant** second video. Both kept + labeled (29939a8d primary). ⚠️ The prior commit **14f44b6** message wrongly says "correct fabricated project id"; that wording is retracted here (history not rewritten).
2. **Shipped the 2-axis differentiator figure** — `deliverables/direction_axis.svg` (effect-size X × signed direction Y): essential TCR machinery (ZAP70/CD3D/LAT, undruggable) vs the druggable **brake** quadrant (CBLB/RASA2/SOCS1/CBL). Rendered to PNG and **eyeballed twice** — fixed opacity/tspan/label-collision issues so it renders cleanly in any rasterizer (solid light tints, no inline tspans). **Clearly labeled SCHEMATIC** (positions illustrative until the CS `--direction` re-run populates real `direction_score`). Wired into `index.html` (new "The differentiator" section) and `demo_storyboard.md` beat 3.
3. **Accuracy fix — closes the pass-#7 minor NEEDS-SAM item.** Softened IL2RB "type-1-diabetes genetics" → "broader autoimmune genetics" across the 5 judge-facing assets (README, summary.md, index.html, hero_convergence.svg, storyboard). Left internal `real-finding-genomescale.md` as Sam set it.

**Decisions:** kept IL2RB **out** of the 2-axis scatter (it's a network nominee, not a direct hit — plotting it would imply a measured effect/direction it doesn't have); noted in the caption instead. Used CD8-brake exemplars (CBLB/RASA2/SOCS1/CBL) as the schematic's "good target" quadrant since they're the real, citable brakes.

### NEEDS SAM (unchanged hard walls)
- **[critical path]** Claude Science `run_pipeline.py --direction` re-run → real `direction_score` CSV (turns the schematic figure into data).
- **[hard walls]** Push public repo + finish video → fill `REPO_URL`/`DEMO_URL`, then submit.
- **[decision]** Hero-arm framing (IL2RB default).

## 2026-07-11T12:40Z (Day 5, 20:40 HKT) — agent build pass: direction.py authored (#1 gap closed in code), IL2RB/VAV2/BLNK dossiers enriched (live OT+CT+PubMed), IL2RB drug overclaim fixed
_Cowork agent, numpy-only sandbox (no scanpy/GPU). Delivered the finishable items; commits **92492fe** (direction.py) + **e2c6a54** (dossiers + doc corrections). All hard walls respected — no submit / no push / no creds / no connectors._

**Did:**
1. **Authored `pipeline/direction.py`** — the carried **#1** method gap (magnitude-only, direction-agnostic ranking). Per-cell `scanpy.tl.score_genes` on the van der Leun/Li *Cell* 2019 CD8 modules (cytotoxic: FGFBP2, GZMB/H, GNLY, PRF1, NKG7, IFNG, GZMA/K, KLRD1; exhaustion: LAG3, PDCD1, HAVCR2, TIGIT, TOX, ENTPD1, CXCL13) → per-(perturbation×donor) signed **`direction_score = cytotoxic − exhaustion`** (Δ vs control, donors as replicates) + **`tier`** (brake / enhancer / required-machinery / neutral) + **per-donor sign-agreement** flag. E-distance stays the magnitude; `viability_ratio` from run_pipeline drives the required-machinery split. PEP8 + type-annotated + frozen dataclasses. A pure-**numpy** `_smoke()` (no scanpy) synthesizes known cytotoxic-up / exhaustion-up perturbations and asserts the sign is recovered — **`python3 pipeline/direction.py` PASSES** (BRAKEGENE +1.99→brake, ENHGENE −2.00→enhancer, NEUTRALGENE→neutral; both donors agree). Wired a guarded **`--direction` flag stub** + comment note into `run_pipeline.py`; **`make smoke` still green**.
2. **Enriched dossiers IL2RB / VAV2 / BLNK** from **live** OT (batched GraphQL — recovered on retry after the global throttle) + ClinicalTrials + PubMed → `dossiers/{IL2RB,VAV2,BLNK}.json` (JSON validated). **CBLB skipped** (already live-OT-enriched earlier today).
   - **IL2RB (hero):** OT = no druggable-family/clinical small molecule, Phase-1 antibody, approved drug only in the "other clinical" bucket = IL-2 agonists. **Corrected the shipped overclaim** in `outputs_gladstone/hero_dossier.md` + `real-finding-genomescale.md`: basiliximab/daclizumab target **IL2RA/CD25, NOT IL2RB**, and **daclizumab was withdrawn 2018** (PMID 29645071); CD122-biased **bempegaldesleukin (NKTR-214)** program **terminated 2022** (22 trials). Genetics = OT overall-assoc immunodeficiency-with-autoimmunity 0.67, MS 0.53 (684 diseases). **Closes the P2 flag from pass #6.**
   - **VAV2:** OT zero small-molecule tractability (undrugged GEF); **genetics NON-immune** (glaucoma/hypertension; T1D only 0.26) → nomination rests on network/paralog coherence, not genetics. Flagged honestly in the finding.
   - **BLNK:** OT zero small-molecule tractability; genetics = **agammaglobulinemia** (0.72) — canonically a **B-cell** adaptor (SLP-76/LCP2 analog) that surfaces in a CD4 screen via shared SYK/PLCG1/ZAP70/LAT neighbours; "antibody-only" overstated (intracellular adaptor). Corrected in the finding.
3. **Consistency verified (no edits needed):** README, `deliverables/summary.md` (Version A = IL2RB PRIMARY; B = Shifrut/CBLB explicit fallback), `index.html`, `demo_storyboard.md`, and `hero_convergence.svg` all headline **IL2RB** coherently. `cd pipeline && python3 edistance_core.py` → **SMOKE TEST PASSED**.

**Decisions:**
- Implemented **exactly the CD8 cytotoxic/exhaustion modules** specified — directly correct for the Shifrut **CD8** arm (CBLB/RASA2). They are top-level swappable constants and `score_direction()` accepts arbitrary per-cell `cell_scores`, so the **CD4 genome-scale** arm can plug in a CD4-appropriate axis (e.g. MSigDB Hallmark `IL2_STAT5_SIGNALING`, per pass #6) **without code changes**.
- Left `REPO_URL` / `DEMO_URL` placeholders untouched (pending Sam; hard walls) — did **not** fabricate URLs.

### NEEDS SAM
- **[critical path] Signed `direction_score` re-run.** direction.py is authored + unit-tested, but regenerating the REAL `ranked_perturbations.csv` with the scanpy `score_genes` column needs a **Claude Science re-run** (~20–40 min; scanpy/GPU absent here). Run `run_pipeline.py --direction` in CS on log-normalized input (`adata.raw` on the X_pca path; supply a CD4-appropriate module for the genome-scale arm). Gates the 2-axis figure + on-camera hero framing.
- **[hard walls] REPO_URL + DEMO_URL:** create/push the public GitHub repo and record the 3-min video, then fill the two placeholders (README, index.html, storyboard) and submit. Agent cannot push / submit / enter creds / authorize connectors.
- **[decision] Hero-arm framing** (IL2RB/CD4 vs CBLB/CD8 vs VAV2) still your call — deliverables default to IL2RB with a labeled Shifrut/CBLB fallback.
- **[minor accuracy]** Deliverables say IL2RB "MS and T1D genetics"; live OT top-2 for IL2RB are immunodeficiency-with-autoimmunity + MS (T1D is pathway/locus-level, not a top-6 OT association). Defensible, but consider softening to "MS + autoimmune."

## 2026-07-11T10:45Z (Day 5, 18:45 HKT) — self-critique pass #6: genome-scale CD4 arm landed; direction axis STILL #1 gap; +1 verified fix for the NEW PPI hub-bias
_Verified live on disk. **Material change since pass #5 (07-10T08:51Z):** the **genome-scale Gladstone arm landed** — `outputs_gladstone/` (real Claude Science run, DGX Spark): **2.44 M primary-human CD4⁺ cells, CRISPRi, 12,449 perturbations (11,438 tested), scVI latent**, plus a **NEW PPI network-propagation layer** (`ppi_propagation*.csv`, `ppi_meta.json`) and a network-nominated hero (**IL2RB**; novel frontier **VAV2/BLNK**) in `real-finding-genomescale.md` + `outputs_gladstone/hero_dossier.md` + `deliverables/`. Big step up. **But the two automated loops now disagree on the hero:** watcher run 5 (10:38Z, above) still says **CBLB** (CD8 Shifrut arm) while the genome-scale finding says **IL2RB/VAV2** (CD4 arm) — a framing decision only Sam can make. And the #1 carried weakness is **still open, now on the genome-scale hero board**; the new PPI layer added a **degree-bias artifact**. Red-team + targeted-research subagents ran in parallel and converged: red-team → direction axis is #1; research → DADA fixes the new hub-bias. Both new citations verified live via PubMed this pass._

### Action items (prioritized)

1. **[P0 — demo-critical; CARRIED #1 from passes #3–#5, CONFIRMED still open, now on the on-camera genome-scale hero board] Add a computed direction-of-effect axis before locking the hero.** No `direction/sign/tier` column exists in **either** output (`outputs/ranked_perturbations.csv` 14 cols; `outputs_gladstone/ranked_perturbations.csv` 19 cols); both rank by |E-distance| only (`run_pipeline.py:144`, `:201`). So the genome-scale top-9 — **ZAP70, LCP2, CD3E, CD3G, PLCG1, LAT, VAV1, SMARCD3, CD3D** — are activation-**required** genes whose KD *cripples* the cell (verified depletion: ZAP70 `viability_ratio` 0.56, CD3E 0.55, CD3G 0.73, PLCG1 0.78), yet they pass all gates and headline the dossier. Therapeutic direction is supplied only by a **hand-typed "Role" column** (`dossiers/SUMMARY.md:19`), not computed. **Fix (two-tier):** (a) *interim, unattended, no GPU* — derive a coarse tier from the existing `viability_ratio` (depleted <~0.8 = KO-impairs/positive-control; enriched >~1.2 = candidate-brake: CBLB 1.32, PDCD1 1.48, DGKA 2.30), split each board, regenerate the hero figure. **Caveat (verified this pass): `viability_ratio` is a NOISY proxy — LAT (1.19) and VAV1 (1.05) are activation-required yet score ≥1, so fitness alone misclassifies them; label it a coarse fitness screen, NOT the effector-direction score.** (b) *rigorous, needs one ~20–40 min Sam Claude-Science re-run* — per-(perturbation×donor) `score_genes` on a **signed effector program**, emit `direction_score` + per-donor sign-agreement, nominate the hero ONLY from the brake tier, ship a 2-axis figure (X=E-dist, Y=signed direction). Same aggregation delivers the README-promised **donor-aware pseudobulk** (closes `README.md:57-58` vs `run_pipeline.py:234` "NEXT" stub). **Note — the genome-scale arm is CD4⁺, so the CD8 exhaustion signature (van der Leun, pass #5) is the wrong axis there;** use a CD4-appropriate readout, e.g. MSigDB Hallmark **`IL2_STAT5_SIGNALING`** (Liberzon et al., *Cell Systems* 2015; PMID 26771021; DOI 10.1016/j.cels.2015.12.004 — verified via PubMed this pass) for the CD4 effector/IL-2 direction; keep van der Leun for the CD8 Shifrut arm.

2. **[P1 — NEW this pass; rigor; the exact published fix for the new PPI layer; agent ~1–2 h, no Sam] De-bias the PPI network-propagation novelty ranking for node degree.** `ppi_meta.json` `top_novel` is **hub-dominated** — verified live: `H4C6, WDR5, H3C13, BRCA1, IL10, H3C12, TAF9, EGFR, TAF10, RPS27A, TAF5, TAF6, MAP3K7, PIK3R3, AR, TAF4, TAF2, H3-4, H3-3B, IL6, … GAPDH` (histones + TAF complex + ribosomal + generic hubs) — while `hero_dossier.md:57` claims the method "rejects hub artefacts." A judge who opens `ppi_meta.json`/`ppi_propagation_full.csv` sees GAPDH/histones as top "novel targets," directly contradicting the narrative. **Fix:** apply **DADA-style degree-aware adjustment** — score each gene's observed personalized-PageRank against its expected value under a **seed-degree-preserving permutation null**, rank by the adjusted z/p, keep raw PPR as a secondary column (DADA can over-correct genuine high-degree signaling genes). *DADA: Degree-Aware Algorithms for Network-Based Disease Gene Prioritization*, Erten, Bebek, Ewing, Koyutürk, **BioData Mining 2011;4:19; PMID 21699738; DOI 10.1186/1756-0381-4-19; PMC3143097** (verified live via PubMed this pass). Makes VAV2/BLNK/IL2RB defensible as **degree-adjusted** nominations rather than raw-walk hubs.

3. **[P1 — the OSS half of the submission; CARRIED #2 from pass #5, STILL open and now far more exposed; agent ~15 min + Sam GitHub] Commit + push; create the public remote; un-ignore the small result artifacts.** `git log` last commit = **03643d2 (Day 3)**; `git remote -v` **empty**; the **entire genome-scale arm is uncommitted/untracked** (`outputs_gladstone/`, `real-finding-genomescale.md`, `deliverables/`, `README.md`, `CONTEXT.md`, updated `run_pipeline.py` + 6 modified files). A judge who clones today gets **Day-3 code and none of the 2.44 M-cell headline result** — and ~2 days of work sits on one disk. (Watcher run 5 flags the CD8-arm slice of this; the genome-scale arm is the bigger exposure.) Un-ignore the small `outputs*/` artifacts (CSVs/figures/JSON; keep `*.h5ad` ignored), commit, create the GitHub remote, push. Off-camera but must land before Mon 21:00 ET.

4. **[P2 — factual accuracy; FLAG only, deliberately NOT auto-fixed] The shipped `hero_dossier.md` still names the wrong IL2RB drugs.** `outputs_gladstone/hero_dossier.md:80` claims IL2RB is drugged by **basiliximab/daclizumab** — but `real-finding-genomescale.md:18` correctly notes those target **IL2RA/CD25, not IL2RB**, and **daclizumab was withdrawn (2018)**. `real-finding` says it "supersedes" the dossier, but the stale claim still ships in the dossier file — an error an immunologist catches instantly. Left untouched (a multi-sentence druggability reframe touching a deliverable + hero-framing judgment ≠ a one-line clearly-correct fix; conservative mandate). **Before shipping:** patch the dossier druggability section to match `real-finding-genomescale.md`, or drop the stale dossier in favor of the corrected doc.

### NEEDS SAM
- **[critical path to the ONLY scored artifact]** Item 1's **rigorous signed `direction_score` re-run** is the one step no agent can do — needs the scanpy/scvi stack + GPU in your Claude Science session (~20–40 min). It gates the hero pick + 2-axis figure, and the demo video (100% of score) can't be locked until it lands. Deadline **Mon 2026-07-13 21:00 ET (~2 days)**. The interim `viability_ratio` tiering (item 1a) de-risks the video even if the GPU re-run slips, but is coarse.
- **[decision-critical, now unavoidable] Which arm is the headline — and which hero?** The two loops have diverged: **CD8 Shifrut → CBLB** (OT-corroborated, clinically de-risked; watcher run 5) vs **CD4 genome-scale → IL2RB/VAV2** (`real-finding-genomescale.md` §"Submission framing" asks you to pick **A) IL2RB** = method-validation, drug-*adjacent* vs **B) VAV2** = bolder novel-hero, harder GEF class). The 3-min demo can open on only one arm+hero — this sets the entire narrative. Can't be auto-resolved.
- **[repro] GitHub handle + create/push the public remote** (item 3) — needs your account; sign-in stays yours.
- **[carried from passes #3–#5] Enhancement-vs-autoimmune concordance sign-anchor still unresolved** — IO/CAR-T enhancement (KO brakes; concordant = autoimmune-`risk_lof`, e.g. CBLB) vs autoimmune dampening. Sets the funnel sign + hero framing.

## 2026-07-11T10:38Z (Day 5, 18:38 HKT) — results-watcher run 5: ✅ Open Targets recovered for the hero (NO new results)
_CSV mtime still 2026-07-09T07:25:41Z — unchanged since run 3 → STEP 1 skipped; non-idle STEP-2 pass._

**Did:** After **5 rate-limited runs, OT briefly recovered** — landed **1 live query before re-throttling**, spent on the hero. **CBLB (ENSG00000114423): REAL OT** — small-molecule tractable (Structure-with-Ligand + High-Quality-Ligand) and **genome-wide-sig genetic association to autoimmune disease** (hypothyroidism 0.81, urticaria 0.74, myxedema 0.69, MS 0.65; 414 diseases). OT-confirms the `risk_lof` brake that was PubMed-only for 5 runs (direction still mechanism-grounded — OT returns association strength, not sign). Also found a **3rd CBL-B program**: **APN401** (NCT06172894, invIOs) ex vivo CBL-B-siRNA cell therapy, **completed Ph1** → CBL-B drugged across 2 modalities. Enriched CBLB.json; appended addenda to DRAFT_finding.md + SUMMARY.md; funnel re-verified live via `concordance.py`.

**Hero → CBLB** (now OT-corroborated). Novelty = RASA2.

**Funnel (REAL, 8 gate-passing hits):** genes 8 / any_gwas 2 / genome_wide_sig 1 / directional 1 / concordant 0 — unchanged.

### NEEDS SAM
- **OT flaky:** only 1/8 genes enriched (hero CBLB) before global re-throttle; the other 7 (RASA2, CD5, TCEB2, CD3D, LCP2, CDKN1B, DGKA) remain PubMed-grounded — retry next run for full direction-of-effect.
- **Sign-anchor (carried, decision-critical):** enhancement-screen convention flips CBLB `risk_lof` → concordant (funnel → 1). Pending your IO-vs-autoimmune anchor call.
- **Uncommitted edits (NOT the watcher's):** `run_pipeline.py` + `real-finding.md` uncommitted since 07-09 — suggest committing so a judge's clone is current.

## 2026-07-10T08:51Z (Day 4, 16:51 HKT) — self-critique pass #5: direction axis STILL the gap; +1 verified new resource
_Verified on disk. Since pass #4 (07-09T08:27Z) and watcher run 4 (07-10T04:09Z): **no analysis re-run** — `outputs/ranked_perturbations.csv` still has **NO direction/sign/tier column** (header re-checked live) and its mtime is unchanged. `git diff HEAD -- run_pipeline.py` confirms the uncommitted edits are only `subsample_per_group` + `--embedding` + `--max-cells-per-group` (scVI/genome-scale plumbing) — **NOT** the pass-#4 P0 direction work, which remains unstarted. Red-team + targeted-research subagents ran in parallel and independently converged: the direction axis is the #1 weakness, and a just-published Nature atlas hands us a benchmark + optional 2nd axis for it. Both new citations re-verified live via PubMed metadata this pass._

### Action items (prioritized)

1. **[P0 — demo-critical; carried from pass #4, CONFIRMED still open; agent ~2–3 h to author + one ~20–40 min Sam CS re-run] The headline ranking is still direction-AGNOSTIC — add the signed axis before locking the hero.** `ranked_perturbations.csv` ranks by |E-distance| only, so the board is **CD3D 9.42 > LCP2 6.65 > RASA2 4.30 > CBLB 4.07 > CD5 3.85** — TCR-*required* genes (whose KO cripples the cell) outranking the actual brakes, with therapeutic direction supplied only by a **hand-typed "Role" column in SUMMARY.md, not computed by the pipeline**. For a method-track entry judged 100% off a video watched by Gladstone immunologists, the one predictable question — "what in your *pipeline* (not your prior) says CBLB beats CD3D?" — has no answer today. **Fix:** per-cell `sc.tl.score_genes` on the van der Leun / Li *Cell* 2019 CD8 modules — cytotoxic (FGFBP2, GZMB/H, GNLY, PRF1, NKG7, IFNG, GZMA/K, KLRD1) vs dysfunction/exhaustion (LAG3, PDCD1, HAVCR2, TIGIT, TOX, ENTPD1, CXCL13; DOI 10.1016/j.cell.2018.11.043) — **aggregate per (perturbation × donor)**, emit a signed `direction_score` + `tier` + per-donor sign-agreement flag, keep E-distance as the magnitude, nominate the hero ONLY from the brake/enhancer tier, ship a 2-axis figure (X = E-dist, Y = signed direction). The same aggregation **delivers the README-promised donor-aware pseudobulk** (closes the claim-vs-code gap: README §Rigor vs `run_pipeline.py` L214 "NEXT" comment) and lets the demo *show* the sign replicating across both donors (softens n=2). Agent can author the code now (no scanpy needed to write it); regenerating the CSV/figure needs Sam's CS run.

2. **[P1 — the OSS deliverable itself; agent ~15 min + Sam GitHub] Commit + push ~2 days of work; un-ignore the small result artifacts; create the public remote.** `git status`: **6 modified + 8 untracked** files uncommitted since the last commit (2026-07-09 14:52); root `.gitignore` L3 = `outputs/`, so the ranked CSV (~2.5 KB) + figures (~130 KB) — the literal *evidence of the finding* — are **absent from any clone** (`git ls-files pipeline/outputs/` is empty); `git remote -v` is **empty (no public remote)**. A judge who clones today gets stale code and none of the real result. **Fix:** un-ignore the small `outputs/` artifacts (keep the h5ad ignored), commit the finding/dossiers/CONTEXT.md, create the public GitHub remote, push. End-loaded on purpose (nothing on camera) but must land before Mon 21:00 ET.

3. **[P1/P2 — NEW verified resource; directly strengthens item 1; agent ~1–2 h] Benchmark the new direction axis on the Chung et al. 2026 _Nature_ atlas; optionally add a 2nd (memory↔exhaustion) axis.** _Atlas-guided discovery of transcription factors for T cell programming_ (Chung, Kaech, Wei Wang et al., **Nature 651:1077–1087, 2026; PMID 41639465; DOI 10.1038/s41586-025-09989-7; PMC13017511** — re-verified live via PubMed) builds a **9-state CD8 atlas** (protective TRM → terminal TEX), runs in vivo Perturb-seq, and nominates TEX-selective TFs **ZSCAN20, JDP2** (+ HIC1, GFI1, KLF6) whose deletion "reduces the expression of inhibitory receptors and improves effector function" **in human T cells**. Use = a **published, human-validated positive-control set**: a correct direction scorer must sign ZSCAN20/JDP2/HIC1/GFI1 "beneficial" — a one-figure, citable proof the new axis works — plus a fresh cell-therapy-relevant novel-target story alongside RASA2. Its **TEX-vs-TRM decoupling** also guards a real failure mode (a KO that collapses *all* differentiation, incl. protective memory, would falsely score "less-exhausted = good"). _Caveat:_ the Perturb-seq is **mouse in vivo** (human = targeted validation), so it complements — not replaces — the primary-human arm; pull the signed gene/TF sets from the Nature supplement (an open bioRxiv preprint reportedly exists — verify the DOI before relying on it). A publisher correction was reported (not independently verified this pass) — check it before quoting exact effect sizes.

4. **[P2 — cheap, safe provenance win; agent ~15 min, +~1 h optional] Cite the now-published pertpy and use it as an independent E-distance cross-check.** _Pertpy: an end-to-end framework for perturbation analysis_ (Heumos et al., **Nat Methods 23:350–359, 2025; PMID 41476114; DOI 10.1038/s41592-025-02909-7** — re-verified live) is the formally-published scverse reference implementation of perturbation E-distance + permutation test — and **Peidli (the scPerturb author already cited) is a co-author**. Add it to `SOURCES.md`/README; if time, run its `pt.tl.Distance`(edistance) as a second implementation that reproduces the `edistance_core` ranking — a strong "independently reproducible" demo beat for ~1 h.

### NEEDS SAM
- **[critical path to the ONLY scored artifact]** The demo video is **100% of the score** and cannot be recorded until the hero + 2-axis figure are locked (item 1). Deadline **Mon 2026-07-13 21:00 ET (~3 days)**. Item 1's **one ~20–40 min Claude Science re-run** — regenerate the Shifrut CSV + figure with the new `direction_score` column, the only step no agent can do (needs the scanpy/pertpy stack) — is the gating dependency. Please prioritize it above further dossier polish.
- **[decision-critical, carried from passes #3–#4] The concordance sign-anchor is still unresolved.** IO/CAR-T **enhancement** (KO brakes; concordant = autoimmune-`risk_lof`, e.g. CBLB) vs **autoimmune dampening** (restore/agonize). It sets item 1's hero framing, the funnel sign, and the demo headline — can't be auto-resolved. Item 1's *screen-direction* read (KO ↑cytotoxicity = brake) lets the hero be nominated on screen-direction even before this is settled, but the funnel sign still needs your call.
- **[repro] GitHub handle / create + push the public remote** (item 2) — needs your account; sign-in stays yours.
- **[carried] Open Targets not retried this pass; per run-4 it has been globally rate-limited across the last 4 consecutive runs** — variant-level direction-of-effect still unconfirmed; CBLB/CD5 signs remain PubMed-grounded.

## 2026-07-10T04:09Z (Day 4, 12:09 HKT) — results-watcher run 4: dossier completion (NO new results)
_`outputs/ranked_perturbations.csv` mtime still 2026-07-09T07:25:41Z — unchanged since run 3 → STEP 1 skipped. Did non-idle prep instead._

**Did:** Built the two remaining gate-passing dossiers — **CDKN1B** (#7, E=1.79) and **DGKA** (#8, E=0.61) — from live ChEMBL v34 + ClinicalTrials.gov v2, taking dossier coverage to **8/8 gate-passing hits**. CDKN1B = undruggable endogenous CDK-inhibitor (0 direct trials; tumor-suppressor/MEN4 → transformation risk). **DGKA correction:** not "preclinical" — Bayer's oral DGKα inhibitor **BAY 2862789 (NCT05858164)** has a **completed Phase 1** (advanced solid tumors/NSCLC, primary completion 2025-09-26). Appended dated addenda to SUMMARY.md + DRAFT_finding.md; re-verified funnel live.

**Expected hero → CBLB** (unchanged): sole gw-sig directional hit, druggable, clinically de-risked. Novelty = RASA2.

**Concordance funnel (real, re-verified via `concordance.py`, 8 gate-passing hits):** genes 8 / any_gwas 2 / genome_wide_sig 1 / directional 1 / concordant 0 — unchanged (both new genes any_gwas=false → no_direction).

### NEEDS SAM
- **Open Targets STILL globally rate-limited — 4th consecutive run, 0 successful calls** (retried at 12s + 25s backoff). OT variant-level direction-of-effect unconfirmed; CBLB/CD5 signs remain PubMed-grounded.
- **Sign-anchor (carried, decision-critical):** the enhancement-screen convention flips CBLB `risk_lof` → concordant (funnel → 1). Pending your IO-vs-autoimmune anchor call.
- **Uncommitted repo edits (NOT the watcher's):** `run_pipeline.py` + `real-finding.md` were changed 2026-07-09 but never committed — likely the pass-#4 direction-of-effect work. If that regenerates the CSV, next run re-runs STEP 1. Suggest committing.

## 2026-07-09T10:08Z (Day 3, 18:08 HKT) — results-watcher run 3: FIRST PASS ON REAL RESULTS (STEP 1)
_`outputs/ranked_perturbations.csv` exists (mtime 07:25:41Z); watcher state had `csv_present:false` at 06:03Z → first watcher pass on real data. Directly executes pass #4's P1 item 2._

**Did:** Built the two missing top-hit dossiers — **CD3D** (rank 1, E=9.42) and **LCP2/SLP-76** (rank 2, E=6.65) — from live ChEMBL + ClinicalTrials: CD3 = approved T-cell-engager class (243 active trials) but agonist/redirect modality; LCP2 = undruggable adaptor (0 drugs, 0 trials). Both labeled **positive controls, not nominees**. Extended `concordance_snapshot.json` (+CD3D/LCP2/CDKN1B/DGKA); refreshed SUMMARY.md + DRAFT_finding.md to real numbers. Cached RASA2/CBLB/CD5 skipped/untouched.

**Expected hero → CBLB** (supersedes UBASH3A, which is absent from this 20-gene panel): top-tier causal KO (E=4.07, all gates pass), sole genome-wide-sig directional hit, `risk_lof` brake, two oral inhibitors in trials — lead + positive control. **RASA2** = novelty (Nature 2022; undruggable → ex vivo KO).

**Concordance funnel (real, verified live via `concordance.py`, 8 gate-passing hits):** genes 8 / any_gwas 2 / genome_wide_sig 1 / directional 1 / concordant 0.

### NEEDS SAM
- **Sign-anchor unresolved (carried, decision-critical):** this is an enhancement screen — counting autoimmune-`risk_lof` as concordant flips CBLB to concordant (funnel → 1). Pending your IO-vs-autoimmune anchor call.
- **Open Targets still globally rate-limited (3rd consecutive run, 0 successful OT calls)** — genetic directions remain PubMed-grounded; OT variant-level direction-of-effect still unconfirmed.

## 2026-07-09T08:27Z (Day 3, 16:27 HKT) — self-critique pass #4: FIRST REAL RUNS LANDED
_Verified on disk — state changed materially since pass #3 (06:05Z). Both real runs now exist (absent every prior pass): `data/DatlingerBock2017.h5ad` (39 MB) and `data/ShifrutMarson2018.h5ad` (871 MB) + `.prepared` copies; `outputs/ranked_perturbations{,_shifrut,_datlinger}.csv` + figures (mtime ~15:2x HKT / ~07:2xZ, ~1h ago); git `03643d2` captured the Datlinger run + concordance module. **The #1 carried blocker from passes #2–#3 ("no real data run has ever executed") is RESOLVED.** Datlinger validates cleanly — top hits recover the TCR-signaling hierarchy (FOS, PTPN6/SHP-1, PTPN11/SHP-2, BACH2, ZAP70, LAT, EGR1/2, NFATC1/3, RELA). Shifrut headline ran: 24,998 cells, 2 donors, control=`control`, 20 perts. This pass critiques the REAL output; the red-team and targeted-research subagents independently converged on the same gap._

### Action items (prioritized)
1. **[P0 — demo-critical; agent ~1.5h + one ~20-min Sam Claude-Science re-run] The headline ranking is direction-AGNOSTIC — add a direction-of-effect axis before the hero is picked.** `ranked_perturbations.csv` ranks by E-distance magnitude only, so the top hits are **CD3D (E=9.42)** and **LCP2/SLP-76 (6.65)** — genes *required* for TCR signaling, whose KO *impairs* activation, i.e. the OPPOSITE of the screen's "KO a brake → cytotoxicity ↑" thesis — outranking the real brakes **RASA2 (4.30)** and **CBLB (4.07)**. Magnitude can't separate brakes from activation-required genes (it can't even tell CD3D/LCP2 apart from the inhibitory co-receptor **CD5** (3.85), whose KO *enhances* signaling), so eyeballing won't do — a principled directional score is needed. **Fix:** score each cell on the **van der Leun / Li et al., _Cell_ 2019** two-axis CD8 signature — cytotoxic module (anchor **FGFBP2**; GZMH, GNLY, GZMB, GZMA, GZMK, PRF1, NKG7, KLRD1, IFNG…) vs dysfunctional/exhaustion module (anchor **LAG3**; PDCD1, HAVCR2, TIGIT, TOX, ENTPD1, CXCL13…) — take per-perturbation pseudobulk Δ(cyto−dysf) vs control, add a **signed column**, tier the leaderboard into *brake/enhancer* vs *required/positive-control*, and nominate the hero ONLY from the brake tier. DOI **10.1016/j.cell.2018.11.043** (PMID 30595452, PMC7253294; full 30-gene lists in Table S8) — citation verified this pass. Agent can add the code + a textbook-direction annotation of the 20 panel genes unattended; regenerating the CSV/figure needs Sam's scanpy stack.
2. **[P1 — the concordance/dossier layer is now STALE vs real hits; agent ~1h] Rebuild dossiers + `concordance_snapshot.json` on the REAL gates-passing genes.** The pre-registered set and the "expected hero **UBASH3A**" are moot on real data: **UBASH3A and TNFAIP3 are not in the Shifrut 20-gene panel at all**, and **SOCS1 has negative E-distance (−0.30) → fails gates** (not a hit). Only CBLB/RASA2/CD5/TCEB2/ARID1A of the 8 snapshot genes overlap, and the funnel's sole "concordant" (UBASH3A) doesn't exist here — the "1 concordant" headline is void. Rebuild on the real brake-tier candidates (**RASA2, CBLB, CD5, TCEB2, CDKN1B, DGKA**) and add the missing-dossier hits **CD3D, LCP2, CDKN1B, DGKA**. Keep the honest coverage funnel; stop carrying UBASH3A as the headline.
3. **[P1 — doc-vs-reality; FLAG only, deliberately NOT auto-rewritten] `real-finding.md` is already stale.** It still frames Datlinger as *the* result and says _"Next: rerun on Shifrut… PTPN11 becomes the positive control"_ — but Shifrut has **already run**. The primary-CD8 headline isn't in the finding doc or the 100–200 w summary yet. Left untouched because folding in the Shifrut result depends on the direction fix + hero pick (items 1–2) — not a one-line correction. Update once item 1 lands.
4. **[P2 — cheap repo/evidence fix; agent ~10 min] Commit the tiny real outputs.** `outputs/` is gitignored, so the ranked CSVs (~2.5 KB) + figures (~130 KB) — the actual *evidence of the finding* — are absent from the repo snapshot a judge clones. Add the small result artifacts (not the h5ad) to git (or a `results/` snapshot dir) so the OSS submission actually contains the finding.
5. **[P2 — carried, now more exposed; cut-able per plan §8] Pseudobulk DE still unimplemented.** `run_pipeline.py` stops at E-distance + viability + knockdown (the "NEXT" comment ~L188) while README §Rigor + SKILL promise donor-aware pseudobulk (Squair 2021 "credibility gate"). With real data flowing and only 2 donors: either implement the minimal version (aggregate per perturbation×donor → pydeseq2, donor-blocked limma fallback) — item 1's direction score should ride the same pseudobulk Δ — or soften README/SKILL to match the code. Don't claim what the code doesn't do.

### NEEDS SAM
- **The enhancement-vs-autoimmune concordance ANCHOR (carried from pass #3) is now decision-critical** — it gates item 1's hero pick and the concordance figure's sign. *Partial relief:* the direction-of-effect axis (item 1) reads the therapeutic direction **directly off the screen** (KO↑cytotoxicity = brake), so the hero can be nominated on screen-direction + tractability even before the genetics-sign convention is settled. But the funnel **sign** still needs the call: **IO/CAR-T enhancement** (KO brakes; concordant = autoimmune-`risk_lof`, e.g. CBLB/RASA2/CD5) **vs autoimmune dampening** (restore/agonize). Pick one — it sets the hero, the funnel, and the demo headline.
- **One ~20-min Claude Science re-run** to regenerate the Shifrut CSV + figure WITH the new direction-of-effect column (item 1). Only step an agent can't do here (needs the scanpy/pertpy stack in your CS session).
- **(Carried, unchanged)** GitHub remote/push for off-machine OSS backup; Open Targets still globally rate-limited → variant-level `directionOnTarget`/`directionOnTrait` still unconfirmed (item 1's directions are literature/textbook until an OT snapshot succeeds).

## 2026-07-09T06:05Z (Day 3) — pass #3 follow-up: QA of the concordance layer (Sam online)
_Confirmed P0 (E-distance `make smoke` passes) + P1 (git `5c681ec`) below — no rework. Funnel math re-checked, correct (8→5→4→4→1). Run 2b's PubMed grounding fixes the *citation* provenance but sharpens, not settles, the sign issue below (OT variant-level direction still missing)._

**The concordance SIGN is anchored wrong for an effector-ENHANCEMENT screen.** `concordance_call` hard-codes `KO → concordant = protective_lof` (`concordance.py:30-31`) — the convention for treating the disease whose GWAS you use. But the screen readout is immune **enhancement** (KO a brake → cytotoxicity ↑) and the genetics is **autoimmune** (the mirror phenotype), so a gene whose LoF boosts effector function reads **`risk_lof`** there. The rule therefore scores the three clean, now-literature-solid brakes **SOCS1 / CBLB / TNFAIP3 as "discordant"** — selecting *against* the exact targets the screen exists to find — and reports "**1 concordant**." Worse, the sole "concordant" hero **UBASH3A** is the mechanistically *ambiguous* one: its `protective_lof` (Ge 2017, T1D-risk allele ↑UBASH3A) implies LoF→↑TCR→↑effector, which should read like the other brakes (risk), yet scores protective via distinct T1D/NF-κB biology — the paradox its own dossier flags. Net: the rule elevates the ambiguous gene and discards the clean ones — the funnel figure + hero a Gladstone immunologist is most likely to challenge.

### Action items
1. **[P1 — rigor; ~1h, gated on the anchor below] Make concordance anchor-explicit.** Add `therapeutic_direction` ("enhance"|"dampen") to `concordance_call`; for an enhancement screen, count autoimmune-`risk_lof` as concordant → funnel becomes ~**3–4 concordant** (SOCS1/CBLB/TNFAIP3), hero reframes to a strong-genetics brake, UBASH3A → hedged. Keep the honest coverage funnel; fix only the sign. Don't hard-code until the anchor is set.
2. **[P1 — provenance] Still no OT variant-level direction** (rate-limited across runs 2/2b). Keep OT `directionOnTarget`/`directionOnTrait` re-enrichment on the list to confirm the PubMed-grounded signs.

### NEEDS SAM
- **Pick the indication anchor — immune ENHANCEMENT (IO/CAR-T: KO brakes) vs AUTOIMMUNE dampening (agonize/restore).** Sets the concordance sign, the concordant gene set, the hero, and the demo headline; can't be auto-resolved. The pipeline currently assumes the autoimmune-native `protective_lof` rule while the whole narrative ("KO → cytotoxicity rises") is enhancement — internally inconsistent. If enhancement: SOCS1/CBLB/TNFAIP3 are your concordant nominations and UBASH3A becomes a sign-ambiguous candidate.

## 2026-07-09T06:03Z (14:03 HKT) — results-watcher run 2b (PubMed grounding)

Grounded run-2's genetic direction-of-effect calls in real literature (PubMed via MCP). Added `dossiers/references.json` + `dossiers/REFERENCES.md` (9 citations, DOI-linked). Highlights: **UBASH3A `protective_lof` confirmed** (Ge et al., Diabetes 2017 — T1D risk allele ↑UBASH3A expression ⇒ loss is protective; the hero's basis); **CBLB `risk_lof` now genome-wide-sig-backed** (Sanna et al., Nat Genet 2010 — rs9657904 P=1.6×10⁻¹⁰ in MS); SOCS1 (Hadjadj, Nat Commun 2020) and TNFAIP3 (Mele, 2014) confirmed `risk_lof`; RASA2 functional/novelty-only (Carnevale, Nature 2022) ⇒ direction `none`; screen lineage = Shifrut et al., Cell 2018. **Concordance funnel unchanged: 8 / 5 / 4 / 4 / 1.** New files only — no plan/framework/report/pipeline source touched (append-only held).

### NEEDS SAM
- Open Targets **still** globally rate-limited (0 successful OT calls across runs 2 + 2b). Genetics now has PubMed grounding but still lacks OT variant-level direction-of-effect confirmation — keep the OT re-enrichment on the list.

## 2026-07-09 (Day 3) — Sam back online; P0 + P1 applied
- **P0 E-distance diagonal-bias bug — FIXED + verified.** `edistance_core.py` within-group terms are now off-diagonal (U-statistic, ÷ n(n−1)). Null E is now ~0 and n-invariant: n=40 → E=−0.28, n=500 → E=0.009 (was ≈5.0 at n=40). Real effect still recovered (E=43.9 ≈ 2‖μ‖²=45, p=0.002). `_smoke()` extended to assert null≈0 at n∈{40,500}; `make smoke` passes. Re-validated the README "strong-120 outranks weak-500" claim = **real biology, not the bug** (STRONG 28.9 ≫ WEAK 1.4 ≫ NULL 0.24 post-fix). Caught by self-critique pass #3.
- **P1 git repo — DONE.** `git init` + `.gitignore` + first commit (local). Public remote/push still needs Sam's GitHub (see NEEDS SAM).
- Still open (needs Sam and/or Claude Science): the first real Datlinger run; wire `concordance.py` into `make hero`; snapshot OT `directionOnTarget`/`directionOnTrait` for the funnel.

## 2026-07-09T05:41Z (13:41 HKT) — results-watcher (CS-independent prep, run 2)

**No real results yet** — `outputs/ranked_perturbations.csv` still absent (no Claude Science run has executed). Ran pre-registered dossier prep on the 8 expected Shifrut/Marson primary-human CD8⁺ KO hits.

**Done this run.** Built 8 target dossiers (`dossiers/<GENE>.json`) with **REAL** ChEMBL v34 + ClinicalTrials.gov v2 data; wrote `dossiers/concordance_snapshot.json` (loads cleanly through `concordance.py`; its self-smoke still passes), `dossiers/SUMMARY.md`, and refreshed `DRAFT_finding.md` + a 3-min demo skeleton. All new files — plan/framework/report/pipeline source untouched (append-only held).

**Concordance funnel (KO, via concordance.py):** genes 8 / any_gwas 5 / genome_wide_sig 4 / directional 4 / **concordant 1**.

**Expected hero candidate: UBASH3A** — the *only* strictly-concordant target (`protective_lof`: the T1D risk allele up-regulates UBASH3A, so loss-of-function is genetically protective), also novel and in a druggable histidine-phosphatase class. **CBLB** = tractability positive control (oral inhibitors NX-1607 Ph1 + HST-1011 Ph1/2 confirmed live in ClinicalTrials). **RASA2** = high-novelty/high-risk (undruggable, no directional genetics). TNFAIP3 (A20, lymphoma tumor-suppressor), ARID1A (tumor-suppressor), TCEB2/ELOB (likely pan-essential) de-prioritized. Everything here is PRE-REGISTERED EXPECTATION, not a result.

### NEEDS SAM
- **Open Targets was rate-limited for the entire run** (global limit; ~9 retries incl. ~80s backoff all failed — consistent with pass #3's finding that the shared bio-research MCPs throttle under load). Genetic-direction + tractability calls are therefore **literature expectation, not OT-confirmed**; schedule an OT re-enrichment pass to replace them. ChEMBL + ClinicalTrials worked fine.
- Still open (unchanged): GitHub handle + repos to index; the autonomy-guardrail decision; install **Claude Science (Max)** + grant `~/Claude Hackathon`, then run the **Datlinger (GSE92872) smoke** to produce the first real `ranked_perturbations.csv` (triggers the real dossier path + E-distance re-ranking).

## 2026-07-09T00:08Z (Day 3) — self-critique pass #3
_Sandbox start 2026-07-09T00:08Z (scheduled 8h fire); entry written 05:40Z — the ~5.5h gap is the targeted-research subagent spending **5.4h on the bio-research MCPs**: live evidence those shared endpoints throttle under load (reinforces the plan's "snapshot Open Targets Day 1, don't live-query at the deadline")._
_**Verified: nothing on disk changed since pass #2** (written 2026-07-08T23:47Z, ~21 min before this run): no `data/`, no `outputs/`, `dossiers/` empty, `.watcher_state.csv_present:false`, all `*.py` untouched. Pass #2's four items (real run · wire concordance · pseudobulk DE · demo rough-cut) **all still open — not re-argued here.** This pass adds two NEW, independently-verified issues._

### Action items (prioritized)
1. **[P0 — CORRECTNESS bug in the scored centerpiece; agent ~1h, no Sam; do BEFORE the first real run] Fix the n-bias in E-distance.** `_mean_sq_pairwise` (`edistance_core.py:22–28`) averages over all n² pairs **including the zero self-distance diagonal**, so the two within-group terms in `e_distance` (`:31–32`) are too small → E is inflated by ≈ **4·d/n** for a null (d=50 PCs). Independently reproduced in-sandbox this pass: null (ctrl-vs-ctrl) mean **E≈5.0 at n=40 vs 0.4 at n=500** (analytic 4d/n = 5.0 / 0.4). Because `power_equalize` subsamples each perturbation to its **own** `min(n_pert,n_ctrl)` (`:35–41`) — not the single "common n" `SKILL.md:30` advertises — this offset **varies per perturbation and corrupts the magnitude ranking, which IS the finding**: a pure-null 40-cell pert scores E≈5 on bias alone, outranking any real effect whose true E-distance < 5, and low-recovery (toxic) KOs get pushed up, blunting the viability gate. **Fix:** off-diagonal-only within term (Σ_{i≠j} / [n(n−1)]) for both within terms; optional hardening = one global common-n subsample; extend `_smoke()` to assert E_null≈0 at n∈{40,500}; re-run `make smoke`. Logged, not applied (unattended/conservative). *After fixing, re-validate the `README.md:51–53` "120-cell hit outranks 500-cell one" line — that behavior is currently produced by the bug, not the biology.*
2. **[P1 — submission + "built-live" compliance; agent ~15 min, no Sam] The folder is not a git repo** (verified: no `.git`). The submission requires an open-source **repository**; ~3 days of solo work is un-snapshotted and there's no commit history to evidence New-Work-Only. **Fix:** `git init` + `.gitignore` (`data/*.h5ad`, `outputs*/`, `__pycache__/`, `*.pyc`) + first commit. (Public remote / off-machine backup still needs Sam's GitHub — see NEEDS SAM.)
3. **[P2 — rigor upgrade, cited, cut-able; ~½ day, no Sam] Mixscale (2025).** Satija-lab method that swaps mixscape's binary perturbed/non-perturbed call for a **continuous per-cell efficiency score used as a DE regression weight** — more power/replication when knockdown is a gradient. *Nat Cell Biol* 27:505–517 ([10.1038/s41556-025-01622-z](https://doi.org/10.1038/s41556-025-01622-z), PMID 40011560); preprint [bioRxiv 2024.01.29.576933](https://doi.org/10.1101/2024.01.29.576933); code github.com/satijalab/Mixscale. **Not yet in pertpy** (scverse #921) → ~50-line adaptation, R-only upstream; best value on **CRISPRi** data (for pure KO Shifrut/Schmidt, binary mixscape stays defensible — frame as robustness, not a swap). Verified across 4 sources. Only pursue if ahead of #1 + the known P0.
4. **[P2 — verification, agent] `make synthetic` has also never run** (`outputs_synth/` absent) — the end-to-end wiring (steps 1–6 → CSV) is unproven even on synthetic data, separate from the missing real run. Correct debug order: **fix #1 → extended `make smoke` → `make synthetic` in any scanpy env → real Datlinger run.** (`make synthetic` needs scanpy/anndata, not the pure-numpy sandbox, but is a Sam-independent smoke of the plumbing.)

_Minor flag: `.watcher_state.json` still reads `last_checked 2026-07-08T10:03Z` although the results-watcher's `lastRunAt` is 2026-07-08T22:00Z and it was due again 04:00Z — the watcher may not be persisting state / pre-building dossiers as intended. Worth a glance, since the dossier automation depends on it._

### NEEDS SAM
- **(THE gate, carried) Install Claude Science (Max) + grant `~/Claude Hackathon`, then run the Datlinger smoke** — unchanged from pass #2, but sharpen the order: apply the agent-fixable **E-distance correction (#1) first** so the debut ranking is correct and isn't thrown away.
- **GitHub handle / create the remote** so #2 becomes a real off-machine OSS repo (also the carried "index prior code" step). Local `git init` I can do unattended; the remote/push is yours.
- _(Pass #2's other open asks — autonomy guardrail, Datlinger control-label + perturbation-key confirmation — still stand; not re-listed.)_

## 2026-07-09T00:00 ET (Day 3) — self-critique pass #2
_ISO ~2026-07-09 (session date); sandbox wall-clock read 2026-07-08T23:46Z — noting the ~1-day skew so timestamps stay trustworthy. ~4 build days left (Thu–Sun), Mon = buffer/submit._

**Since Day 2:** `concordance.py` landed + unit-tests (was the #1 MAJOR TODO — good). **Still zero real-data runs:** `pipeline/outputs/` absent, `.watcher_state.json` `csv_present:false`. `concordance.py` is still an island (not imported by `run_pipeline.py`, not in `make hero`; run_pipeline.py still marks concordance + pseudobulk DE "NEXT" ~L188). No Open Targets snapshot; no pseudobulk DE; no demo artifact.

### Action items (prioritized)
1. **[P0 — TODAY] Land the first real end-to-end run on Datlinger (GSE92872, ~39 MB), NOT the 871 MB Shifrut.** Red-team's single #1 risk, and the un-actioned Day-2 priority: the demo is 100% of the score in both rounds and there is *no* real finding to show; the data→env→run→output path has never once executed (highest-probability point of total failure with ~4 days solo). Steps: download `DatlingerBock2017.h5ad` from scPerturb Zenodo → `pipeline/data/`; build env (`make env`) **or** run inside Claude Science for the provenance trail; add a `smoke-real` Makefile target; `make hero DATA=data/DatlingerBock2017.h5ad CONTROL=<confirm>`; sanity-check that ZAP70/LCK/LAT KO blunt the TCR signature and that `outputs/ranked_perturbations.csv` is written (trips the results-watcher). ETA 0.5–1 day. *Only then* scale to Shifrut (the Gladstone-grade primary-CD8 headline) and wire concordance.
2. **[P1] Wire `concordance.py` in AND populate it from Open Targets' pre-computed direction-of-effect fields.** New this pass: OT GraphQL `Evidence` now exposes **`directionOnTarget`** (LoF/GoF) + **`directionOnTrait`** (risk/protective) — introduced release 24.03 (Mar 2024), agent-verified against the live schema this session (`variantEffect` is NOT a valid field — use these exact names). So instead of hand-deriving directionality, gate on OT's own harmonized call: a KO/CRISPRi (LoF) hit is concordant when `directionOnTarget=LoF` AND `directionOnTrait=protective`. Map to `concordance.py`'s `direction` ('protective_lof'/'risk_lof'); snapshot to a JSON that `load_snapshot()` reads; add the step to `make hero`. The OT query add is ~1h and the snapshot can be prepped in parallel now (query the ~29 Datlinger genes / expected top hits) — but the funnel only produces output once #1 yields a ranked CSV. Method paper (npj Drug Discovery 2025, DOI 10.1038/s44386-025-00027-0) is *already* cited in README §Rigor — what's new is the **usable API fields**, not the paper.
3. **[P2 — rigor; scope to fit clock] Donor-aware pseudobulk DE.** SKILL step 6 / README §4 promise it (Squair 2021 pseudoreplication "credibility gate"), but `run_pipeline.py` stops at E-distance + viability + knockdown — a docs-vs-code overclaim the CS reviewer and judges will catch. Implement minimal: aggregate raw counts per (perturbation×donor) → pydeseq2, with a donor-blocked limma/t-test fallback if pydeseq2 is heavy for the timeline. Cuttable per plan §8 — but if cut, soften the README/SKILL wording to match (don't claim what the code doesn't do).
4. **[P2 — demo insurance] Throwaway 60-sec narrative rough-cut** (plan schedules it for today). The moment #1 yields *any* real artifact, lock the story skeleton — problem → E-distance ranking → concordance figure → hero + proposed experiment → provenance/one-command rerun. Cheap; directly de-risks the single highest-leverage deliverable.

### NEEDS SAM
- **Install Claude Science (Max) + grant `~/Claude Hackathon`, then run the Datlinger smoke (#1).** Still the top blocker — carried from Day 2, and the red-team confirms CS install is unconfirmed. Without it, #1 must fall back to a local `make env` conda build. This one decision gates the entire demo.
- **Confirm the Datlinger control label + perturbation key before running #1.** CROP-seq Jurkat may not use `NT` / `perturbation`; SKILL step-1 audit resolves it — flag so `--control` isn't guessed and the run doesn't fail on an assert.
- **(Carryover, still open — not re-arguing here)** Autonomy-guardrail acceptance (agent drives only the dossier layer) + GitHub handle for prior-code indexing. Lower priority than #1; revisit after a real run exists.

## 2026-07-08 (Day 2) — first self-critique pass

### Fixed this session (code — all validated in-sandbox)
- `run_pipeline.py::effect_sizes` — viability now computed for **all** perturbations **before** the low-cell drop, relative to the **median** perturbation size (was: `len(Xp)/pooled_NT`, which tripped almost everything and hid depleted KOs). Depleted perts are surfaced with a flag + `note`, not silently dropped. Added asserts: control label exists and has ≥ `min_cells`.
- `run_pipeline.py::knockdown_check` — **modality-aware** (KO/CRISPRi/CRISPRa); proper per-cell-normalized fold-change on raw counts. KO `on_target_ok` = NaN because Cas9 KO mRNA often doesn't drop (NMD escape) — prove editing via guide-calls/functional signature instead. Added `--modality`.
- `score_and_rank` — NaN-safe; toxic (viability) and depleted (NaN) perts fail gates; strong clean hit ranks first. Verified.
- Added `LICENSE` (MIT) + `SOURCES.md` — OSS compliance was a stated disqualifier and was missing.
- `README` — corrected the `make hero` overclaim (regenerates the ranked shortlist; concordance+hero module is next, not built yet).

### High-priority next (build order)
1. **[MAJOR]** Concordance funnel + pseudobulk DE are still `TODO` in `run_pipeline.py` — that's the *scored differentiator*. Build `concordance.py` against a **snapshotted** Open Targets table for the top ~10 genes; pseudobulk via pydeseq2 (fallback: donor-blocked limma/t-test); wire into `make hero`. ETA D3–D4.
2. **[MAJOR]** No real-data run has executed yet. Dry-run the **full** path on the 39 MB **Datlinger (GSE92872)** set end-to-end **today** to de-risk OOM/label/timeout before the 871 MB Shifrut run. ETA D1–D2.
3. **[UPGRADE — promote to "never cut"]** **Bidirectional opposite-modality replication**: validate the hero/top-k with **Schmidt 2022 CRISPRa** — activation should move the effector program *opposite* to Shifrut KO. Neutralizes the n=2-donor weakness and is the single best demo figure (one gene, two opposing arrows, two independent labs). Cheap tier = Schmidt hit-table sign check (~½ day). Refs: Science abj4008; Nat Biotechnol nbt.4062; Sci Rep s41598-017-18172-6.

### Autonomy decision (from the red-team)
- **Verdict: do NOT let an agent drive Claude Science's single-cell runs.** It would confidently execute any remaining bug and write authoritative artifacts (the CS reviewer checks claims-vs-code, not method *validity*); it erases the human-in-the-loop provenance story that **is** the scored deliverable; unattended it can loop on the 871 MB job or hammer rate-limited endpoints (the $200 burn); GUI-clicking is brittle.
- **Recommended guardrail:** autonomy ONLY on the API-shaped **dossier enrichment** (the results-watcher) with host allowlist + token/$ cap + skip-if-exists. Single-cell runs = **one stage per message**, eyeball each artifact — that per-stage gate is both the bug-catch and the provenance story.
- Note: the desktop-control access request **timed out this session** (not granted).

### NEEDS SAM
- **GitHub handle + repos** to index your prior code (blocking the "index GitHub first" step).
- **Autonomy call:** accept the guardrail (agent drives only the dossier layer; you run the sc-stages one-per-message in Claude Science), or still want me to attempt GUI-driving once CS is installed?
- Install **Claude Science (Max)** + grant `~/Claude Hackathon`, then run the **Datlinger smoke**.
